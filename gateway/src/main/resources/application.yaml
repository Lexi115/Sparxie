spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            # Game
            - id: game
              uri: ${GAME_SERVICE_BASE_URI:http://game:8080}
              predicates:
                - Path=/api/game/**
              filters:
                - StripPrefix=1
            # Banner
            - id: banner
              uri: ${BANNER_SERVICE_BASE_URI:http://game:8080}
              predicates:
                - Path=/api/banner/**
              filters:
                - StripPrefix=1
            # Shop
            - id: shop
              uri: ${SHOP_SERVICE_BASE_URI:http://game:8080}
              predicates:
                - Path=/api/shop/**
              filters:
                - StripPrefix=1
