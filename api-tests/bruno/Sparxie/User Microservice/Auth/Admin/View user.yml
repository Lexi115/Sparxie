info:
  name: View user
  type: http
  seq: 4

http:
  method: GET
  url: "{{userBaseUri}}/auth/admin/users/{{userId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
