info:
  name: Refresh token
  type: http
  seq: 2

http:
  method: POST
  url: "{{userBaseUri}}/auth/refresh"
  body:
    type: json
    data: |-
      {
        "refreshToken": "{{refreshToken}}"
      }
  auth: inherit

runtime:
  variables:
    - name: userId
      value: e2217fe7-2978-4595-a489-0490e6b72c62
    - name: refreshToken
      value: boia

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
