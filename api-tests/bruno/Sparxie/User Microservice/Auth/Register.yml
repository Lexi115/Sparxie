info:
  name: Register
  type: http
  seq: 3

http:
  method: POST
  url: "{{userBaseUri}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "{{username}}",
        "email": "{{email}}",
        "password": "{{password}}"
      }
  auth: inherit

runtime:
  variables:
    - name: username
      value: sparkle
    - name: password
      value: feetus
    - name: email
      value: a@a.it

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
