info:
  name: Get profile by ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{userBaseUri}}/profiles/admin/users/{{userId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
