info:
  name: Get authenticated user
  type: http
  seq: 1

http:
  method: GET
  url: "{{userBaseUri}}/user/me"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
