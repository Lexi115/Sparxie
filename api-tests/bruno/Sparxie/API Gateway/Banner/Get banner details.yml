info:
  name: Get banner details
  type: http
  seq: 2

http:
  method: GET
  url: "{{gatewayBaseUri}}/api/banner/details/{{bannerId}}"
  auth: inherit

runtime:
  variables:
    - name: bannerId
      value: banner_weapon_event_sparkle_01

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
