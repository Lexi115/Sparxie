info:
  name: Perform purchase
  type: http
  seq: 1

http:
  method: POST
  url: "{{gatewayBaseUri}}/api/game/purchases"
  body:
    type: json
    data: |-
      {
        "transactionId": "{{$randomUUID}}",
        "itemId": "{{itemId}}",
        "amount": {{itemAmount}}
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: itemId
      value: oneiric_shard
    - name: itemAmount
      value: "100"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
