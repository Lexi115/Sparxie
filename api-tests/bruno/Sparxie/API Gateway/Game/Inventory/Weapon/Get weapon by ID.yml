info:
  name: Get weapon by ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{gatewayBaseUri}}/api/weapon/{{weaponId}}"
  auth: inherit

runtime:
  variables:
    - name: weaponId
      value: lc_earthly_escapade

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
