info:
  name: Delete account
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{gatewayBaseUri}}/api/auth/account"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
