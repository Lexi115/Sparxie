info:
  name: Delete user
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{gatewayBaseUri}}/api/auth/admin/users/{{userId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
