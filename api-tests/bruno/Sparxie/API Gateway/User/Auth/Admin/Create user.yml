info:
  name: Create user
  type: http
  seq: 2

http:
  method: POST
  url: "{{gatewayBaseUri}}/api/auth/admin/users"
  body:
    type: json
    data: |-
      {
        "username": "{{$randomUserName}}",
        "email": "{{$randomEmail}}",
        "isEmailConfirmed": true,
        "password": "{{$randomPassword}}",
        "roles": [
          "ADMIN", "MEMBER"
        ]
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
