info:
  name: Authorize (OAuth2)
  type: http
  seq: 6

http:
  method: GET
  url: "{{gatewayBaseUri}}/api/auth/authorize/{{provider}}"
  auth: inherit

runtime:
  variables:
    - name: provider
      value: github

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
