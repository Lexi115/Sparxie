info:
  name: Perform warp
  type: http
  seq: 1

http:
  method: POST
  url: "{{gameBaseUri}}/game/warp/pull"
  body:
    type: json
    data: |-
      {
        "transactionId": "{{$randomUUID}}",
        "playerId": "{{playerId}}",
        "bannerId": "{{bannerId}}",
        "amount": {{pullAmount}}
      }
  auth: inherit

runtime:
  variables:
    - name: playerId
      value: eb0fdb32-9aa0-44b8-985c-fc63d339898d
    - name: bannerId
      value: banner_character_event_sparkle_01
    - name: pullAmount
      value: "10"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
