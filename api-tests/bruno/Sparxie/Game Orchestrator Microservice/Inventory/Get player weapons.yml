info:
  name: Get player weapons
  type: http
  seq: 3

http:
  method: GET
  url: "{{gameBaseUri}}/inventories/weapons?page={{page}}"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  params:
    - name: page
      value: "{{page}}"
      type: query
    - name: size
      value: "{{size}}"
      type: query
      disabled: true

runtime:
  variables:
    - name: page
      value: "0"
    - name: size
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
