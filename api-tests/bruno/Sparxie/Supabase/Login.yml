info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{supabaseUri}}/token?grant_type=password"
  params:
    - name: grant_type
      value: password
      type: query
  body:
    type: json
    data: |-
      {
        "email": "a@a.it",
        "password": "sparxie"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
