info:
  name: Signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{supabaseUri}}/signup"
  body:
    type: json
    data: |-
      {
        "email": "ba.it",
        "password": "sparkle"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
