info:
  name: Get player purchase history
  type: http
  seq: 1

http:
  method: GET
  url: "{{loggerBaseUri}}/histories/purchases/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
